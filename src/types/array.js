var arr = ["Henry","Clint","CJT","Richard"]
var join1 = arr.join()
var join2 = arr.join("")
console.log(join1)
console.log(join2)
//the parameter of the join is the element between each word, the default is comma